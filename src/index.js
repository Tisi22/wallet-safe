import React from 'react';
import ReactDOM from 'react-dom';
import AppUnity from './App.tsx';
import OpenSafeBox from './SafeBox.tsx';

ReactDOM.render(
  <React.StrictMode>
    <OpenSafeBox />
  </React.StrictMode>,
  document.getElementById('root')
);
